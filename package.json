{
  "name": "openstatus-railway",
  "version": "0.1.0",
  "private": true,
  "description": "Railway template that self-hosts OpenStatus (status pages + uptime monitoring) fully inside one Railway project",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "plan": "railway config plan",
    "apply": "railway config apply"
  },
  "devDependencies": {
    "railway": "^3.11.0"
  },
  "engines": {
    "node": ">=22"
  }
}
